<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Litmas feat. Sleigher</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #6e0f0f;
      color: #f5cc59;
      font-family: 'Impact', 'Segoe UI', sans-serif;
      text-align: center;
      overflow: hidden;
    }

    nav {
      background: #000;
    }

    .navbar-brand,
    .nav-link {
      color: #f5cc59 !important;
      font-weight: bold;
      font-size: 1rem;
      letter-spacing: 1px;
    }

    .hero {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 1rem;
      background-color: #6e0f0f;
      position: relative;
    }

    .krampus-img {
      max-height: 180px;
      margin-bottom: 1rem;
    }

    .qr-code {
      width: 140px;
      height: 140px;
      background-color: #5c0d0d;
      padding: 8px;
      margin-bottom: 1rem;
    }

    .date {
      font-size: 1.25rem;
      letter-spacing: 1px;
    }

    .burning-tree {
      position: absolute;
      bottom: 0;
      left: 50%;
      transform: translateX(-50%);
      height: 100px;
      opacity: 0.7;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand fixed-top py-2">
    <div class="container-fluid justify-content-center">
      <ul class="navbar-nav">
        <li class="nav-item mx-2"><a class="nav-link" href="#rsvp">RSVP</a></li>
        <li class="nav-item mx-2"><a class="nav-link" href="#bottlerocket">Bottlerocket</a></li>
        <li class="nav-item mx-2"><a class="nav-link" href="#sleigher">Sleigher</a></li>
      </ul>
    </div>
  </nav>

  <!-- Hero -->
  <div class="hero">
    <img src="{{ asset('assets/hyper_link/krampus.png') }}" alt="Krampus" class="krampus-img" />
    <div>LITMAS</div>
    <div>NOVEMBER 29, 2025</div>
  </div>

</body>
</html>
