<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Projects</title>
  <meta name="description" content="Your description here.">
  <style>
    /* Reset & Base */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      line-height: 1.6;
      color: #b7b09d;
      background-color: #2E241B;
      min-height: 100vh;
    }

    a {
      color: #b7b09d;
      text-decoration: none;
      transition: color 0.2s;
    }

    a:hover {
      color: #F6D58A;
    }

    /* Layout */
    .container {
      max-width: 1280px;
      margin: 0 auto;
      padding: 3rem 1.5rem;
    }

    @media (min-width: 768px) {
      .container {
        padding: 4rem 3rem;
      }
    }

    /* Header */
    .back-link {
      display: inline-flex;
      align-items: center;
      font-weight: 600;
      font-size: 0.875rem;
      color: #F6F1EB;
      margin-bottom: 0.5rem;
      transition: transform 0.2s;
    }

    .back-link:hover {
      transform: translateX(-4px);
      color: #F6D58A;
    }

    .back-link svg {
      width: 1rem;
      height: 1rem;
      margin-right: 0.25rem;
      transform: rotate(180deg);
    }

    h1 {
      font-size: 2.5rem;
      font-weight: bold;
      color: #F6F1EB;
      margin-bottom: 3rem;
    }

    @media (min-width: 640px) {
      h1 {
        font-size: 3rem;
      }
    }

    /* Table */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 3rem;
    }

    thead {
      position: sticky;
      top: 0;
      z-index: 10;
      border-bottom: 1px solid rgba(246, 241, 235, 0.1);
      background-color: rgba(46, 36, 27, 0.75);
      backdrop-filter: blur(10px);
    }

    th {
      padding: 1rem 2rem 1rem 0;
      text-align: left;
      font-size: 0.875rem;
      font-weight: 600;
      color: #F6F1EB;
    }

    th:last-child {
      padding-right: 0;
    }

    tbody tr {
      border-bottom: 1px solid rgba(246, 241, 235, 0.1);
    }

    tbody tr:last-child {
      border-bottom: none;
    }

    tbody tr:hover {
      background-color: rgba(246, 213, 138, 0.05);
    }

    td {
      padding: 1rem 2rem 1rem 0;
      vertical-align: top;
      font-size: 0.875rem;
    }

    td:last-child {
      padding-right: 0;
    }

    .project-name {
      font-weight: 600;
      font-size: 1rem;
      color: #F6F1EB;
      display: inline-flex;
      align-items: baseline;
      gap: 0.25rem;
    }

    .project-name:hover {
      color: #F6D58A;
    }

    .project-link {
      display: inline-flex;
      align-items: baseline;
      color: #726D5F;
      font-size: 0.875rem;
    }

    .project-link:hover {
      color: #F6F1EB;
    }

    .external-icon {
      width: 1rem;
      height: 1rem;
      margin-left: 0.125rem;
      display: inline-block;
      transition: transform 0.2s;
    }

    a:hover .external-icon {
      transform: translate(2px, -2px);
    }

    /* Tags */
    .tags {
      display: flex;
      flex-wrap: wrap;
      gap: 0.375rem;
      list-style: none;
    }

    .tag {
      background-color: rgba(232, 180, 75, 0.1);
      color: #e8b44b;
      padding: 0.25rem 0.75rem;
      border-radius: 9999px;
      font-size: 0.75rem;
      font-weight: 500;
    }

    /* Responsive */
    .hide-mobile {
      display: none;
    }

    @media (min-width: 640px) {
      .hide-mobile {
        display: table-cell;
      }
    }

    @media (min-width: 1024px) {
      .hide-tablet {
        display: table-cell;
      }
    }

    .hide-tablet {
      display: none;
    }

    /* Year column */
    .year {
      white-space: nowrap;
      color: #b7b09d;
    }
  </style>
</head>
<body>
  <div class="container">
    <a href="/" class="back-link">
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M3 10a.75.75 0 01.75-.75h10.638L10.23 5.29a.75.75 0 111.04-1.08l5.5 5.25a.75.75 0 010 1.08l-5.5 5.25a.75.75 0 11-1.04-1.08l4.158-3.96H3.75A.75.75 0 013 10z" clip-rule="evenodd"/>
      </svg>
      Your Name
    </a>
    <h1>All Projects</h1>
    
    <table>
      <thead>
        <tr>
          <th>Year</th>
          <th>Category</th>
          <th>Project</th>
          <th class="hide-tablet">Made at</th>
          <th>Built with</th>
          <th class="hide-mobile">Link</th>
        </tr>
      </thead>
      <tbody>
        {% for project in projects %}
        {% if project.category not in ["Play", "Podcast"] %}
        <tr>
          <td class="year">{{ project.year }}</td>          
          <td class="year">{{ project.category }}</td>
          <td>
            {% if project.url %}
            <a href="{{ project.url }}" class="project-name" target="_blank" rel="noreferrer noopener">
              {{ project.name }}
              <svg class="external-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M5.22 14.78a.75.75 0 001.06 0l7.22-7.22v5.69a.75.75 0 001.5 0v-7.5a.75.75 0 00-.75-.75h-7.5a.75.75 0 000 1.5h5.69l-7.22 7.22a.75.75 0 000 1.06z" clip-rule="evenodd"/>
              </svg>
            </a>
            {% else %}
            <span class="project-name">{{ project.name }}</span>
            {% endif %}
          </td>
          <td class="hide-tablet">{{ project.madeAt }}</td>
          <td>
            <ul class="tags">
              {% for tag in project.tags %}
              <li class="tag">{{ tag }}</li>
              {% endfor %}
            </ul>
          </td>
          <td class="hide-mobile">
            {% if project.url %}
            <a href="{{ project.url }}" class="project-link" target="_blank" rel="noreferrer noopener">
              {{ project.url|replace({'https://': '', 'http://': ''})|split('/')|first }}
              <svg class="external-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M5.22 14.78a.75.75 0 001.06 0l7.22-7.22v5.69a.75.75 0 001.5 0v-7.5a.75.75 0 00-.75-.75h-7.5a.75.75 0 000 1.5h5.69l-7.22 7.22a.75.75 0 000 1.06z" clip-rule="evenodd"/>
              </svg>
            </a>
            {% endif %}
          </td>
        </tr>
        {% endif %}
        {% endfor %}
      </tbody>
    </table>
  </div>
</body>
</html>
