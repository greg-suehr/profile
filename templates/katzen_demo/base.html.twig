<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Katzen</title>
  <meta name="description" content="ERP for cottage kitchens, bakeries, and small food businesses. Track inventory, manage recipes, handle orders, and understand your costs.">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: system-ui, sans-serif;
      line-height: 1.6;
      color: #5A4436;
      background-color: #F6F1EB;
    }

    a {
      color: #5A4436;
      text-decoration: none;
    }

    a:hover {
      color: #E8B44B;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }

    .hero {
      position: relative;
      min-height: 500px;
      background: linear-gradient(135deg, #2E241B 0%, #5A4436 100%);
      display: flex;
      align-items: center;
      overflow: hidden;
    }

    .hero-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(46, 36, 27, 0.15);
    }

    .hero-content {
      position: relative;
      z-index: 1;
      color: #F6F1EB;
      padding: 4rem 0;
    }

    .hero-title {
      font-size: 3rem;
      font-weight: bold;
      margin-bottom: 0.75rem;
      text-shadow: 0 2px 8px rgba(46, 36, 27, 0.3);
    }

    .hero-tagline {
      font-size: 1.25rem;
      text-shadow: 0 1px 4px rgba(46, 36, 27, 0.3);
    }

    .hero-card {
      position: absolute;
      bottom: 2rem;
      right: 2rem;
      max-width: 380px;
      background: rgba(253, 252, 249, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
      color: #5A4436;
    }

    @media (max-width: 768px) {
      .hero-card {
        position: relative;
        bottom: auto;
        right: auto;
        margin: 2rem auto 0;
        max-width: 100%;
      }
    }

    section {
      padding: 4rem 0;
    }

    .section-title {
      font-size: 1.75rem;
      font-weight: bold;
      color: #5A4436;
      margin-bottom: 0.75rem;
      text-align: center;
    }

    .section-subtitle {
      color: #7a6a5a;
      text-align: center;
      margin-bottom: 3rem;
      max-width: 700px;
      margin-left: auto;
      margin-right: auto;
    }

    .cta-card {
      background: linear-gradient(135deg, #F6F1EB 0%, #E5DED1 100%);
      border-radius: 12px;
      padding: 3rem;
      text-align: center;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    }

    .cta-card h2 {
      color: #5A4436;
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }

    .cta-card p {
      color: #7a6a5a;
      max-width: 600px;
      margin: 0 auto 1.5rem;
    }

    .btn-primary {
      display: inline-block;
      background: #E8B44B;
      color: #2E241B;
      padding: 1rem 2.5rem;
      border-radius: 8px;
      font-weight: 600;
      transition: all 0.2s ease;
    }

    .btn-primary:hover {
      background: #d4a23d;
      color: #2E241B;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(232, 180, 75, 0.4);
    }

    .benefits-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.5rem;
      margin-bottom: 3rem;
    }

    @media (max-width: 768px) {
      .benefits-grid {
        grid-template-columns: 1fr;
      }
    }

    .benefit-card {
      background: #FDFCF9;
      border-radius: 12px;
      padding: 1.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
    }

    .benefit-icon {
      width: 48px;
      height: 48px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      color: white;
      font-size: 1.25rem;
    }

    .benefit-icon.gold { background: #E8B44B; }
    .benefit-icon.olive { background: #A7A17E; }
    .benefit-icon.plum { background: #845D7E; }

    .benefit-card strong {
      display: block;
      color: #5A4436;
      margin-bottom: 0.25rem;
    }

    .benefit-card span {
      color: #7a6a5a;
      font-size: 0.875rem;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1.5rem;
    }

    @media (max-width: 768px) {
      .features-grid {
        grid-template-columns: 1fr;
      }
    }

    .feature-card {
      background: #FDFCF9;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
    }

    .feature-header {
      display: flex;
      align-items: flex-start;
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .feature-icon {
      width: 56px;
      height: 56px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      color: white;
      font-size: 1.5rem;
    }

    .feature-icon.sizzle { background: linear-gradient(135deg, #F6D58A 0%, #E8B44B 100%); }
    .feature-icon.tabby { background: linear-gradient(135deg, #D1CEB2 0%, #A7A17E 100%); }
    .feature-icon.ledger { background: linear-gradient(135deg, #B89DB5 0%, #845D7E 100%); }
    .feature-icon.binx { background: linear-gradient(135deg, #9aac8e 0%, #7bb373 100%); }

    .feature-title {
      font-size: 1.125rem;
      font-weight: 600;
      color: #5A4436;
      margin-bottom: 0.25rem;
    }

    .feature-subtitle {
      font-size: 0.875rem;
      color: #7a6a5a;
    }

    .feature-card > p {
      color: #7a6a5a;
      font-size: 0.9375rem;
    }
  </style>
</head>
<body>
  {% block main_content %}
  <p>This page intentionally left blank.</p>
  {% endblock %}
</body>
</html>
