{% extends 'hyper_link/base.html.twig' %}

{% block content %}
    <div class="scene" id="birthdayConfirm">
        <div class="dialogue">
            <p class="character-dialogue" id="dialogue1">So, are you coming?</p>
        </div>
        <div class="choices hidden">
            <a class="choice canonical" href="{{ path('hyperlink_jump', { nextNodeKey: 'birthdayYes' }) }}">Yes!</a>
            <a class="choice" href="{{ path('hyperlink_jump', { nextNodeKey: 'birthdayWhen' }) }}">When it it?</a>
            <a class="choice" href="{{ path('hyperlink_jump', { nextNodeKey: 'birthdayWhere' }) }}">Where it it?</a>            
        </div>
    </div>
{% endblock %}

